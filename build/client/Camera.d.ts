export declare class Camera {
    x: number;
    y: number;
    w: number;
    h: number;
    constructor(x: number, y: number, w: number, h: number);
    setTarget(): void;
    getDetails(): void;
}
